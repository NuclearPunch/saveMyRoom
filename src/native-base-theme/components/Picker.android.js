// @flow

import variable from "../variables/platform";

export default (variables /*: * */ = variable) => {
  const pickerTheme = {
    ".note": {
      color: "#8F8E95",
      width: 90,
    marginRight: -4,
    borderWidth:10,
    borderRadius: '4px',
    borderColor:'#cccccc'
    },
    
  };

  return pickerTheme;
};
